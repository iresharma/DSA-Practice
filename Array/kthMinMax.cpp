// Find the "Kth" min and max on the array

#include<iostream>
using namespace std;

int main() {
    #ifndef ONLINE_JUDGE
        freopen("../input.txt", "r", stdin);
        freopen("../output.txt", "w", stdout);
    #endif
    int size, position;
    int arr[50];
    cin>>size;
    cin>>position;
    for (int i = 0; i < size; i++)
    {
        cin>>arr[i];
    }
    
    return 0;
}